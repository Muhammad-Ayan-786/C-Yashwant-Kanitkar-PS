/*
[B] Point out the errors, if any, in the following programs:

(a) main( ) {
        float a = 12.25, b = 12.52 ;
        if ( a = b )
            printf ( "\na and b are equal" ) ;
    }

(b) main( ) {
        int j = 10, k = 12 ;
        if ( k >= j ) 
        {
            {
                k = j ;
                j = k ;
            }
        }
    }

(c) main( ) {
        if ( 'X' < 'x' )
            printf ( "\nascii value of X is smaller than that of x" ) ;
    }

(d) main( ) {
        int x = 10 ;
        if ( x >= 2 ) then
            printf ( "\n%d", x ) ;
    }

(e) main( ) {
        int x = 10 ;
        if x >= 2
        printf ( "\n%d", x ) ;
    }

(f) main( ) {
        int x = 10, y = 15 ;
        if ( x % 2 = y % 3 )
            printf ( "\nCarpathians" ) ;
    }

(g) main( ) {
        int x = 30 , y = 40 ;
        if ( x == y )
            printf( "x is equal to y" ) ;
        elseif ( x > y )
            printf( "x is greater than y" ) ;
        elseif ( x < y )
            printf( "x is less than y" ) ;
    }

(h) main( ) {
        int x = 10 ;
        if ( x >= 2 ) then
            printf ( "\n%d", x ) ;
    }

(i) main( ) {
        int a, b ;
        scanf ( "%d %d",a, b ) ;
        if ( a > b ) ;
            printf ( "This is a game" ) ;
        else
            printf ( "You have to play it" ) ;
}

*/

#include<stdio.h>

int main() {

    printf("There is an error in problem (d). \n");
    printf("There is an error in problem (e). \n");
    printf("There is an error in problem (f). \n");
    printf("There is an error in problem (g). \n");
    printf("There is an error in problem (h). \n");
    printf("There is an error in problem (i). \n");

    return 0;
}